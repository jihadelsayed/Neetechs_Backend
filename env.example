# Django
SECRET_KEY=change-me
DEBUG=False
ALLOWED_HOSTS=server.neetechs.com
CSRF_TRUSTED_ORIGINS=https://server.neetechs.com

# Database (choose one approach)
DATABASE_URL=postgres://neetechs:change-me@127.0.0.1:5432/neetechs
# Or explicit fields (if not using DATABASE_URL)
DB_NAME=neetechs
DB_USER=neetechs
DB_PASSWORD=change-me
DB_HOST=127.0.0.1
DB_PORT=5432

# Email
EMAIL_HOST=smtp.gmail.com
EMAIL_HOST_USER=info@neetechs.com
EMAIL_HOST_PASSWORD=change-me
EMAIL_PORT=587
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=info@neetechs.com

# CORS (comma-separated list)
CORS_ALLOWED_ORIGINS=https://server.neetechs.com,http://localhost:4200

# Stripe (optional)
STRIPE_SECRET_KEY=
STRIPE_PUBLIC_KEY=

# AWS S3 (optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=

# Django Channels (if using Redis)
REDIS_URL=redis://127.0.0.1:6379/0
CHANNEL_LAYERS_BACKEND=channels_redis.core.RedisChannelLayer
